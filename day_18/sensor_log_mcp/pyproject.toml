[project]
name = "sensor-log-mcp"
version = "0.1.0"
description = "An MCP server that stores temperature samples in SQLite: save a sample, list sensors, list a sensor's last samples."
requires-python = ">=3.11"
dependencies = ["mcp>=2.2.0"]
